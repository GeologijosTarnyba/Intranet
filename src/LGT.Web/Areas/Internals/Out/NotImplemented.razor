@page "/darboviete/{ParamStringID}"
@page "/asmuo/{ParamIntID}"

@code {
  [Parameter] public string? ParamStringID { get; set; }
  [Parameter] public string? ParamIntID { get; set; }
}

<h4>Puslapis nepadarytas, esant poreikiui bus padaryta.</h4>