@inherits Create2<Darboviete, LGT.Intranet.Internal.InternalContext>
@code {
  #region Constructors
  public CreateDarboviete()
  {
    this.ParamInit = (prev) => new Darboviete()
    {
    };
  }
  #endregion
}
@if (this.model != null)
{
  <CreateFormatTable Model=@this.model OnValidSubmit=@this.DoCreate>
    <InputString ParamName="ID" @bind-value=@model.ID />
    <InputString ParamName="Name" @bind-value=@model.Name />
  </CreateFormatTable>
}
